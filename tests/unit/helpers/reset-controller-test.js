import { resetController } from 'borrowers/helpers/reset-controller';
import { module, test } from 'qunit';

module('Unit | Helper | reset controller');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resetController([42]);
  assert.ok(result);
});
